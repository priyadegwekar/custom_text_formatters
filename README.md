# Custom Text Formatter

## Description
Provides a Slugify field formatter for text fields.

## Features
- Custom field formatter for text fields
- Uses cocur/slugify service

## Installation
1. Enable module
2. Run composer install
3. Clear cache

## Usage
Manage display → Select "Slugify" formatter → Configure separator
